---
description: 클린 아키텍처 기반 메뉴 엔티티 리팩토링 제안서
---

# 🏗 메뉴 엔티티 클린 아키텍처 설계도 (Blueprint)

이 문서는 `Menu` 엔티티와 관련 컴포넌트들을 클린 아키텍처 원칙에 따라 리팩토링하기 위한 설계도를 담고 있습니다. 핵심 비즈니스 로직을 외부 프레임워크, 데이터베이스, UI 관심사로부터 분리하는 것을 목표로 합니다.

## 1. 목표
- **디커플링(Decoupling)**: 비즈니스 로직이 JDBC, Spring 또는 특정 외부 라이브러리에 의존하지 않도록 합니다.
- **테스트 용이성(Testability)**: 데이터베이스나 웹 서버 없이도 핵심 로직을 테스트할 수 있게 합니다.
- **유지보수성(Maintainability)**: 관심사를 명확히 분리하여 구현체 변경(예: JDBC를 JPA로 교체)을 용이하게 합니다.

## 2. 제안하는 패키지 구조

기존의 평면적인 구조에서 계층 기반의 계층적 구조로 변경합니다.

```text
com.new_cafe.app
├── domain (도메인 계층: 엔티티, 포트)
│   └── menu
│       ├── model
│       │   └── Menu.java             <-- 핵심 엔티티 (순수 자바)
│       └── port
│           ├── in
│           │   └── MenuUseCase.java  <-- 입력 포트 (서비스 인터페이스)
│           └── out
│               └── MenuRepository.java <-- 출력 포트 (리포지토리 인터페이스)
├── application (애플리케이션 계층: 유스케이스 구현)
│   └── menu
│       └── service
│           └── MenuService.java      <-- MenuUseCase의 구현체
├── infrastructure (인프라 계층: 영속성 어댑터)
│   └── persistence
│       └── menu
│           └── MenuPersistenceAdapter.java <-- MenuRepository의 구현체
└── web (인터페이스 어댑터 계층: 컨트롤러, DTO)
    └── menu
        ├── controller
        │   └── MenuController.java
        └── dto
            ├── MenuRequest.java
            └── MenuResponse.java
```

## 3. 컴포넌트 상세 설명

### A. 도메인 계층 (Core Layer)
- **Menu.java**: `korName`, `price` 등의 속성을 가진 순수 자바 클래스입니다. `@Entity`나 `@Table` 같은 외부 어노테이션을 가지지 않습니다.
- **MenuRepository (출력 포트)**: 도메인 계층이 데이터베이스와 소통하는 방식을 정의하는 인터페이스입니다 (예: `save`, `findById`).
- **MenuUseCase (입력 포트)**: 외부(웹 등)에서 비즈니스 로직을 호출하기 위해 사용할 인터페이스입니다 (예: `createMenu`, `getMenuList`).

### B. 애플리케이션 계층 (Application Layer)
- **MenuService**: `MenuUseCase`를 구현합니다. 데이터가 어떻게 저장되는지(JDBC/JPA 등)는 모르며, `MenuRepository` 인터페이스를 사용하여 비즈니스 흐름을 제어합니다.

### C. 인프라 계층 (Infrastructure Layer)
- **MenuPersistenceAdapter**: `MenuRepository`를 실제로 구현합니다. 여기에 실제 JDBC나 JPA 코드가 위치하며, 도메인 객체(`Menu`)와 DB 엔티티/결과셋 사이의 매핑을 담당합니다.

### D. 웹 계층 (Web Layer)
- **MenuController**: HTTP 요청을 처리합니다.
- **MenuResponse/Request**: 웹 인터페이스 전용 DTO입니다. 컨트롤러는 이 DTO를 도메인 객체로 변환하여 서비스를 호출합니다.

## 4. 리팩토링 단계

1. **1단계: 도메인 정의**
   - 외부 의존성 없는 `domain.menu.model.Menu` 생성
   - 도메인 전용 `domain.menu.port.out.MenuRepository` 정의

2. **2단계: 유스케이스 구현**
   - `domain.menu.port.in.MenuUseCase` 인터페이스 정의
   - 이를 구현하는 `application.menu.service.MenuService` 구현

3. **3단계: 영속성 어댑터 구현**
   - 기존 `NewMenuRepository`의 JDBC 로직을 `infrastructure.persistence.menu.MenuPersistenceAdapter`로 이동
   - 해당 어댑터가 도메인 포트를 구현하도록 수정

4. **4단계: 웹 계층 조정**
   - 기존 컨트롤러가 새로운 서비스를 호출하도록 업데이트하고 DTO 매핑 로직 적용

---

*이 설계도는 리팩토링 과정을 안내하는 가이드라인입니다. 승인 후 디렉토리 구조 생성 및 코드 이동을 진행하겠습니다.*
